# 长播客总结质量提升方案

## 当前问题
- **输入**: 144,267 字符
- **输出**: 6,550 字符
- **压缩比**: 4.5%（远低于目标的25%）
- **质量**: 输出过短，可能遗漏重要信息

---

## 方案对比

### 方案1: 强制分块生成（推荐 ⭐⭐⭐⭐⭐）
**策略**: 对于片段数 > 50 的播客，强制使用分块生成

**流程**:
1. 将73个ASR分段合并成15-20个块（每块约4-5个片段，保持语义边界）
2. 每个块独立生成总结（maxTokens: 8000，确保每个块有足够输出）
3. 合并所有块的总结
4. 最终整合：将合并结果再次生成最终报告（maxTokens: 32000）

**优点**:
- ✅ 质量高：每个块都能充分处理，不会因为输入过长而压缩输出
- ✅ 成本适中：约20-25次API调用（15-20块 + 1次最终整合）
- ✅ 稳定性好：分块处理，单个块失败不影响整体
- ✅ 输出长度可控：每个块输出约2000-3000字符，总计30-40K字符

**缺点**:
- ⚠️ 处理时间稍长：约60-80分钟（但质量优先）
- ⚠️ API调用次数多：约20-25次

**成本估算**:
- API调用：20-25次
- Token消耗：约150K-200K tokens（输入+输出）
- 成本：约 ¥15-20（基于通义千问定价）

**效果预期**:
- 输出长度：30,000-40,000 字符（20-28%压缩比）
- 质量：高（每个块都能充分展开）

---

### 方案2: 两阶段生成（平衡方案 ⭐⭐⭐⭐）
**策略**: 先整体生成大纲，再分块填充细节

**流程**:
1. 第一阶段：整体生成大纲（输入完整ASR，输出5-8K字符的大纲）
2. 第二阶段：根据大纲，分块生成详细内容（15-20块，每块2000-3000字符）
3. 合并所有详细内容

**优点**:
- ✅ 质量较高：有整体视角，又有细节展开
- ✅ 成本较低：约16-21次API调用
- ✅ 输出结构清晰：有明确的大纲和详细内容

**缺点**:
- ⚠️ 实现复杂：需要两阶段协调
- ⚠️ 可能重复：大纲和详细内容可能有重叠

**成本估算**:
- API调用：16-21次
- Token消耗：约120K-160K tokens
- 成本：约 ¥12-16

**效果预期**:
- 输出长度：25,000-35,000 字符（17-24%压缩比）
- 质量：较高

---

### 方案3: 优化提示词 + 强制输出长度（低成本 ⭐⭐⭐）
**策略**: 改进提示词，明确要求输出长度，并添加输出长度检查

**流程**:
1. 在提示词中明确要求：输出必须达到ASR原文的25%（36K字符）
2. 添加输出长度验证：如果输出 < 20%，触发分块生成
3. 如果整体生成失败或输出过短，自动切换到分块生成

**优点**:
- ✅ 成本最低：优先尝试整体生成（1次API调用）
- ✅ 实现简单：只需修改提示词和添加验证逻辑
- ✅ 向后兼容：不影响短播客的处理

**缺点**:
- ⚠️ 效果不确定：AI可能仍然输出过短
- ⚠️ 需要验证：需要测试AI是否真的能输出36K字符

**成本估算**:
- API调用：1-2次（如果失败则切换到分块）
- Token消耗：约90K-150K tokens
- 成本：约 ¥9-15

**效果预期**:
- 输出长度：20,000-36,000 字符（14-25%压缩比）
- 质量：中等-高（取决于AI是否遵循长度要求）

---

### 方案4: 智能分块（自适应方案 ⭐⭐⭐⭐）
**策略**: 根据ASR片段数自动选择策略

**流程**:
- 片段数 ≤ 30：整体生成
- 片段数 31-50：优化提示词 + 整体生成（带长度检查）
- 片段数 > 50：强制分块生成（方案1）

**优点**:
- ✅ 自适应：根据播客长度选择最优策略
- ✅ 成本可控：短播客成本低，长播客质量高
- ✅ 平衡效率和质量

**缺点**:
- ⚠️ 实现稍复杂：需要判断逻辑

**成本估算**:
- 短播客（≤30片段）：1次API调用，约 ¥5-8
- 中播客（31-50片段）：1-2次API调用，约 ¥8-12
- 长播客（>50片段）：20-25次API调用，约 ¥15-20

**效果预期**:
- 短播客：15-25%压缩比
- 长播客：20-28%压缩比

---

## 推荐方案

### 🏆 首选：方案1（强制分块生成）
**理由**:
- 质量最高，输出长度可控
- 成本适中（¥15-20）
- 稳定性好，已有分块生成代码基础

### 🥈 备选：方案4（智能分块）
**理由**:
- 自适应，平衡效率和质量
- 成本可控
- 用户体验好（短播客快，长播客质量高）

---

## 实施建议

如果选择方案1，需要修改：
1. `src/server/audio-processor.ts`: 添加片段数判断逻辑
2. `src/clients/report-generator.ts`: 优化分块生成的提示词和输出长度
3. 测试：用146分钟播客验证效果

如果选择方案4，需要修改：
1. `src/server/audio-processor.ts`: 添加智能判断逻辑
2. `src/clients/report-generator.ts`: 优化各策略的提示词
3. 测试：用不同长度的播客验证效果




